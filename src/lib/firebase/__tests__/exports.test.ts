import { 
  getProjectDocuments, 
  uploadProjectDocument, 
  type ProjectDocument, 
  type UploadMetadata 
} from '../index';

// This is just to verify the exports exist
console.log({
  getProjectDocuments,
  uploadProjectDocument
}); 